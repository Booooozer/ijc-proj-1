#include <stdio.h>
#include "bitset.h"

int main() {
    unsigned long velikost = 100;
    bitset_create(p,100)
    bitset_alloc(o, 200)

    printf("%lu\n",p[0]);

    printf("P size = %lu\n", sizeof(p));
    printf("P size = %lu\n", sizeof(o));

    bitset_free(o)
    return 0;
}
